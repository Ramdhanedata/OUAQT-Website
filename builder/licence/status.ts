/*
 * The licence rules live in app-ui now, because the desktop app has to apply
 * exactly the same ones with no network, and two copies of a rule about when
 * a shop stops being able to sell would drift apart the first time one of
 * them was fixed.
 */
export * from "@/app-ui/licence-status";
