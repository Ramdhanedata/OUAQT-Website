# Copy this file to `.env.local` and fill in real values.
# `.env.local` is gitignored — never commit real secrets.

# Public site URL, used for metadata/OG tags (app/layout.tsx)
NEXT_PUBLIC_SITE_URL=https://ouaqt.com

# TODO(customize): add a key here when you wire up the contact form
# (e.g. Resend, Formspree, or your own API route). The form in
# components/contact/contact-form.tsx currently has no backend.
# RESEND_API_KEY=
# CONTACT_TO_EMAIL=ouaqt.mrt@gmail.com
